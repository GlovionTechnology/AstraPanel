╔═══════════════════════════════════════════════════════════════╗
║         🚀 ASTRAPANEL PHASE 4 - DEPLOYMENT CHECKLIST         ║
╚═══════════════════════════════════════════════════════════════╝

📋 PRE-DEPLOYMENT CHECKLIST
══════════════════════════════════════════════════════════════════

✅ STEP 1: LOCAL TESTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Build frontend:
    cd client
    npm run build

□ Test production mode:
    Windows: ./build-and-run.bat
    Linux/Mac: ./build-and-run.sh

□ Verify at http://localhost:3000
    ✓ Login page loads
    ✓ Dashboard works
    ✓ No console errors

══════════════════════════════════════════════════════════════════

✅ STEP 2: GITHUB SETUP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ GitHub repository:
    URL: https://github.com/GlovionTechnology/AstraPanel
    Visibility: Private
    Already configured!

□ Push code (use GIT_COMMANDS.txt):
    git init
    git add .
    git commit -m "Phase 4: Production Ready"
    git branch -M main
    git remote add origin https://github.com/GlovionTechnology/AstraPanel.git
    git push -u origin main

□ Verify on GitHub:
    ✓ All files uploaded (except node_modules, .env)
    ✓ install.sh is visible

══════════════════════════════════════════════════════════════════

✅ STEP 3: VPS PREPARATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Get VPS:
    Providers: DigitalOcean, Vultr, Linode, AWS EC2
    Minimum: 1GB RAM, 1 CPU
    OS: Ubuntu 20.04 or 22.04

□ SSH into VPS:
    ssh root@YOUR_VPS_IP

□ Update system:
    apt update && apt upgrade -y

══════════════════════════════════════════════════════════════════

✅ STEP 4: INSTALLATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Run installer:
    curl -fsSL https://raw.githubusercontent.com/GlovionTechnology/AstraPanel/main/install.sh | bash

□ Wait for installation (5-10 minutes)

□ Check PM2 status:
    pm2 status

□ Check Nginx:
    sudo systemctl status nginx

══════════════════════════════════════════════════════════════════

✅ STEP 5: FIRST ACCESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Open in browser:
    http://YOUR_VPS_IP

□ Login with:
    Username: admin
    Password: admin123

□ IMMEDIATELY change password in Settings

══════════════════════════════════════════════════════════════════

✅ STEP 6: SECURITY (RECOMMENDED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ Setup firewall:
    sudo ufw allow 22/tcp
    sudo ufw allow 80/tcp
    sudo ufw allow 443/tcp
    sudo ufw enable

□ Install SSL (if you have domain):
    sudo apt install certbot python3-certbot-nginx
    sudo certbot --nginx -d yourdomain.com

□ Change default passwords

══════════════════════════════════════════════════════════════════

📝 IMPORTANT FILES CREATED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ server/index.js         → Updated for production
✓ .gitignore              → Updated with AstraPanel specifics
✓ install.sh              → VPS auto-installer
✓ build-and-run.bat       → Windows production test
✓ build-and-run.sh        → Linux/Mac production test
✓ DEPLOYMENT.md           → Full deployment guide
✓ GIT_COMMANDS.txt        → Quick git reference
✓ .env.example            → Environment variables template

══════════════════════════════════════════════════════════════════

🔧 USEFUL COMMANDS (ON VPS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
pm2 status                 → Check server status
pm2 logs astrapanel        → View live logs
pm2 restart astrapanel     → Restart server
pm2 stop astrapanel        → Stop server

sudo systemctl restart nginx  → Restart Nginx
sudo nginx -t              → Test Nginx config

cd /opt/astrapanel         → Go to installation directory

══════════════════════════════════════════════════════════════════

⚠️  BEFORE YOU START
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❗ Test locally first (Step 1)
❗ Update install.sh with YOUR GitHub username
❗ Never commit .env files
❗ Keep GitHub repo Private
❗ Change default passwords immediately

══════════════════════════════════════════════════════════════════

🎉 NEXT STEPS AFTER DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Create your first website through the panel
2. Point your domain to VPS IP
3. Setup SSL certificates
4. Configure automated backups
5. Monitor system resources

══════════════════════════════════════════════════════════════════

📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
→ Full Guide: DEPLOYMENT.md
→ Git Commands: GIT_COMMANDS.txt
→ Environment Setup: .env.example

══════════════════════════════════════════════════════════════════

Ready to deploy? Follow each step carefully! 🚀
